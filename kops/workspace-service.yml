apiVersion: v1
kind: Service
metadata:
  name: workspace
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v1
      kind:  Mapping
      name:  workspace_mapping
      prefix: /api/workspace/
      service: workspace:8080
spec:
  type: ClusterIP
  ports:
  - port: 8080
    name: http
  selector:
    app: workspace
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workspace
spec:
  selector:
    matchLabels:
      app: workspace
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: workspace
        version: '7'
    spec:
      containers:
      - name: workspace
        image: jasonblanchard/jawn-workspace:7
        env:
          - name: 'PORT'
            value: '8080'
          - name: 'MONGO_HOST'
            value: 'mongo'
          - name: 'DB_NAME'
            value: 'jawn'
          - name: 'LOG_LEVEL'
            value: 'DEBUG'
          - name: 'JWT_SECRET'
            valueFrom:
              secretKeyRef:
                name: app-secrets
                key: jwtSecret
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
             path: /health
             port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          successThreshold: 1
